<li id="users-<%= user.id %>">
  <span class="name">
    <%= "#{ user.first_name } #{user.last_name}"%>
    <%= link_to "Delete", delete_team_path(@team, :user_id => user.id), method: :delete %>
  </span>
</li>